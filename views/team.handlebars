{{> header}}
<p>Hola soy donde van a ir la lista de equipos</p>

{{#if equipo}}
<form action = " " method="POST">
    <label><h3>TLA: <input type="text" name="tla" value="{{equipo.tla}}" maxlength="3" placeholder="Maximo de 3 caracteres"></h3></label>
    <label><h3>Nombre: <input type="text" name="name" value="{{equipo.name}}"></h2></label>
    <label><h3>Nombre corto: <input type="text" name="short-name" value="{{equipo.shortName}}"></h3></label>
  <div >
    <button type="submit" name="guardar">Guardar</button>
    <a href="/">Volver</a>
  </div>
</form>

{{else}}
        <h3>No team found!</h3>
{{/if}}